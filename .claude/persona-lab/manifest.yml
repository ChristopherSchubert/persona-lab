grain: single
owner: Chris
bus: github-issues
repo: persona-lab
engagement:
  developer:          { capacity: writes }
  product-manager:    { capacity: doc-writes }
  product-analyst:    { capacity: owns }
  lead-engineer:      { capacity: audits }
  platform-architect: { capacity: doc-writes }
  enterprise-architect: { capacity: doc-writes }
  delivery-manager:   { capacity: doc-writes }
  data-architect:     { capacity: doc-writes }
  head-of-security:   { capacity: doc-writes }
  security-analyst:   { capacity: audits }
  head-of-qa:         { capacity: doc-writes }
  qa-analyst:         { capacity: audits }
  head-of-design:     { capacity: doc-writes }
  design-analyst:     { capacity: audits }
  finops:             { capacity: owns }
  marketing:          { capacity: doc-writes }
  technical-writer:   { capacity: doc-writes }
  release-engineer:   { capacity: audits }
  reliability-engineer: { capacity: reads }
  accessibility-analyst: { capacity: reads }
  privacy-analyst:    { capacity: reads }
oversight:
  autonomy: conservative
  visibility: minimal
