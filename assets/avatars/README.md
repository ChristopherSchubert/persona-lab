# Persona avatars

Per-individual pixel-art PNGs, one per name (see ../../docs/personas/name-pools.md).
Filename = lowercase ASCII name, e.g. ben.png, esme.png. PNG only (GitHub renders PNG, not SVG,
in issue comments). Square pixel-art, ~64-128px native; crisp at 16-20px.
